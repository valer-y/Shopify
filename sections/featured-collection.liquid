{% for product in section.settings.faetured_collection.products %}
    {%  render 'product-card', product_product: product %}    
{% endfor %}

{% schema %}
    {
        "name": "Featured collection",
        "settings": [{
            "type": "text",
            "id": "title",
            "default": "Featured collection",
            "label": "Titile"
        },
        {
            "type": "collection",
            "id": "featured_collection",
            "label": "Collection"
        }
        ]
    }
{% endschema %}